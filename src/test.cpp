#include "deque.h"
#include "heap.h"
#include "vector.h"
#include <algorithm>
#include <assert.h>
#include <deque>
#include <iostream>
#include <list>
#include <new>
#include <type_traits>
#include <utility>
#include <vector>

// tinystl::vector<int> x3(tinystl::vector<int>{2, 4, 6});??
// std::vector<int> x {1,2,3,4,5,6};tinystl::vector<int>
// x1(x.begin()+1,x.end()); ??
// tinystl::vector<int> x{1, 2, 3, 4, 5, 6};x.insert(x.begin() + 1, x.begin(),
// x.begin() + 3);??
int main() {
  // tinystl::deque<int> x1(1025);

  int n;
  int x[n];
  return 0;
}